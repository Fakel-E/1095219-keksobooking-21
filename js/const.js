'use strict';

(() => {
  const INDEX_MIN = 0;
  const NOT_VALID_REPORT = `Количество гостей больше, чем количество комнат`;
  const MOUSE_BUTTON = 1;

  window.const = {
    INDEX_MIN,
    NOT_VALID_REPORT,
    MOUSE_BUTTON,
  };
})();
